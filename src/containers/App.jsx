import { useState } from 'react';
import AnimalDisplay from '../components/AnimalDisplay';

const App = () => {
    return (
        <div>
            <AnimalDisplay />
        </div>
    );
};

export default App;
